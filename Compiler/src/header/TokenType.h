#pragma once

enum TokenType
{
	INTEGER,
	BOOLEAN,
	DATA_TYPE,
	ARITHMETIC_OPERATOR,
	PRIMITIVE_KEYWORD,
	PARENTHESIS,
	COMMA,
	COLON,
	IDENTIFIER,
	COMPARATOR,
	LOGICIAL_OPERATOR,
	CONDITIONAL,
	COMMENTOR
};
